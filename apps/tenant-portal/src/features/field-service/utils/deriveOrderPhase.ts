/**
 * @deprecated Import from `deriveOrderWorkflow`.
 *
 * Kept as a compatibility entry point for extensions that still use the old
 * filename. There is only one workflow implementation.
 */
export * from './deriveOrderWorkflow'
export { deriveOrderWorkflow as deriveOrderPhase } from './deriveOrderWorkflow'
