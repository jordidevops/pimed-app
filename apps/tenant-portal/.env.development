# Entorno: DEVELOPMENT (local)
# Este archivo SÍ puede estar en git — solo contiene URLs de localhost.
# Para staging/prod: copia .env.example a .env.staging / .env.production

VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_PUBLISHABLE_DEFAULT_KEY=sb_publishable_ACJWlzQHlZjBrEguHvfOxg_3BJgxAaH
VITE_SUPPORT_EMAIL=support@example.app
VITE_APP_DISPLAY_NAME=Your App
VITE_PORTAL_DNS_CNAME_TARGET=proxy.public.example.com
VITE_PORTAL_DNS_TXT_PREFIX=_portal-verify


VITE_SIGNING_HTML_PDF_OUTPUT=true

# Portal d'empleat (public-portal local) — base per construir URLs /e/{secret}
VITE_PUBLIC_PORTAL_BASE_URL=http://localhost:3002

# URL base del customer-portal (butlletins /s/{secret} i staff /staff/{secret})
VITE_CUSTOMER_PORTAL_ORIGIN=http://localhost:3003